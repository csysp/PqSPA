table inet fw4 {
  set wg_spa_allow {
    type ipv4_addr;
    flags timeout;
  }

  chain input {
    # Allow WireGuard UDP only if source IP is in the SPA allow set
    # Replace 51820 with your WireGuard UDP port
    udp dport 51820 ip saddr @wg_spa_allow accept
  }
}

